{
  "name": "tixy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel ./src/index.html --open",
    "clean": "rm -Rf dist/",
    "build": "parcel build ./src/index.html ./src/assets/twitter-card.png",
    "firebase": "firebase deploy --only hosting",
    "deploy": "npm run clean && npm run build && npm run firebase"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel-plugin-inline-source": "^1.0.0",
    "stylus": "^0.54.8"
  },
  "dependencies": {
  }
}
